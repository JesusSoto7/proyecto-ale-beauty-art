<article id="banner-home">
    <%= image_tag "home.png", class: "logo-img" %>
</article>

<ul class="ubi">
    <%= link_to "Home", home_path %> / 
    <%= link_to "Categorias", categories_path, class: "ubi-active" %>
</ul>

<div>
    <% content_for :title, "Categories"%>

    <h1> Categorias </h1>

    <div id="order">
        <% @categories.each do |category| %>
            <%= render category %>
        <% end %> 
    </div>
</div>
