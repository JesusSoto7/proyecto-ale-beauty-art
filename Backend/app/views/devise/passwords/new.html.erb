<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title></title>
    <link rel="stylesheet" href="app/assets/stylesheets/pasword.css">
  
  </head>
  <body class="body">

   <section class="container">
      <%= image_tag 'ale_logo.jpg', alt: 'logo', class: 'logo' %>


      <h2 class="titulo">¿Olvidó su contraseña?</h2>
      <p class="texto">Ingrese su correo electrónico y le enviaremos instrucciones para recuperarsu contraseña.</p>

      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div class="field">
          <%= f.label :email %><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
        </div>
        <div class="options">
          <%= render "devise/shared/links" %>
        </div>
        <div class="boton">
          <%= f.submit "Enviar" %>
        </div>
      <% end %>
       
    </section>  
  </body>    
</html>      
